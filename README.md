# meadhall-modpack

Server-authoritative modpack manifest for our Mead Hall Valheim group.
Edited by the `meadhall-server` CLI (`update`/`apply`), not by hand, once Phase 4 lands.
Server IP and password are never stored here.
